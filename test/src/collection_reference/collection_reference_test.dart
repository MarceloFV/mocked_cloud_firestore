import 'package:flutter_test/flutter_test.dart';
import 'package:mocked_cloud_firestore/src/collection_reference/collection_reference.dart';

void main() {
  MkCollectionReference reference;
  group('Collection Reference parameters test', () {
    setUp(() {
      // reference = MkCollectionReference(id: 'aaa');
    });
    test('Collection Reference test', () {
      // var id = reference.id;
      // expect(id, 'aaa');
    });
  });
}
